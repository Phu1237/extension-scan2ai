export default function () {
  const isFullPage = false;
  const capturePlace = 'background';
  const captureType = 'visible';

  return {
    isFullPage: isFullPage,
    capturePlace: capturePlace,
    captureType: captureType
  };
}
